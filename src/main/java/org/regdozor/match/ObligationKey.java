package org.regdozor.match;

/**
 * Составной ключ для поиска в таблице обязанностей: пара (группа, обязанность).
 *
 * Зачем отдельным record'ом: {@link java.util.Map} ищет по ОДНОМУ ключу, а нам нужна ПАРА. record
 * склеивает два поля в одно значение с готовыми {@code equals}/{@code hashCode} «по значениям» —
 * поэтому {@code new ObligationKey("одежда","нанесение")} находит запись, хотя это каждый раз новый
 * объект. По одной «одежде» статью не опознать (она в обеих записях) — различает обязанность, оттого
 * ключ и парный.
 *
 * @param group      товарная группа ("одежда")
 * @param obligation обязанность ("нанесение" / "ГИС МТ")
 */
public record ObligationKey(String group, String obligation) {
}
