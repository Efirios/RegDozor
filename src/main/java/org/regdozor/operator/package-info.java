/**
 * Источники ОПЕРАТОРА маркировки (ЦРПТ) — ярус 2 в модели источников проекта.
 *
 * Сюда попадают ОБА сайта оператора, потому что организация одна:
 * <ul>
 *   <li><b>честныйзнак.рф</b> — лента «Что нового в системе» ({@link org.regdozor.operator.CrptReleaseNotesParser},
 *       {@link org.regdozor.operator.CrptReleaseExtractor}): системные новости ЦРПТ, изменения перечня там не живут;</li>
 *   <li><b>markirovka.ru</b> — статьи сообщества ({@link org.regdozor.operator.MarkirovkaCommunityParser},
 *       {@link org.regdozor.operator.MarkirovkaReleaseExtractor}): готовые таблицы кодов ОКПД2+ТН ВЭД и даты волн.</li>
 * </ul>
 *
 * Общее для обоих живёт здесь же: {@link org.regdozor.operator.ArticleExtractor} (шов «сайт-специфичное
 * извлечение текста») и {@link org.regdozor.operator.ArticleTextFetcher} (скачать + разобрать, одинаково для всех).
 * Разнести сайты по разным пакетам нельзя без третьего дома для этого шва.
 *
 * ⚠️ Ярусы источников (не путать): ярус 1 — {@code org.regdozor.pravo}, первоисточник (публикация актов
 * и текст из ЭБПИ); ярус 2 — ЭТОТ пакет, оператор, цитировать можно с атрибуцией; ярус 3 — коммерческие
 * блоги, только для навигации, цитировать нельзя.
 *
 * ⚠️ Приставка {@code Crpt} в именах классов означает УЗКО честныйзнак.рф (лента релизов), а не всю
 * организацию — из-за этой двусмысленности пакет и переименован из {@code crpt} в {@code operator}.
 */
package org.regdozor.operator;
