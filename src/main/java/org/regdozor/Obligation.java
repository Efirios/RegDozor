package org.regdozor;

/**
 * Одна ОБЯЗАННОСТЬ по товару. У одного товара их несколько (напр. «нанесение средств идентификации»
 * и «внесение сведений в ГИС МТ») — и у каждой СВОЯ дата и СВОЙ риск, поэтому это отдельная запись,
 * а не поля прямо в Product. Ключевое доменное правило: «даты принадлежат обязанности, а не товару».
 *
 * @param what      что за обязанность (напр. "Нанесение средств идентификации")
 * @param since     дата вступления в силу как строка ISO ("2025-03-01")
 * @param source    основание — ПОСТАНОВЛЕНИЕ (ППРФ), которое ввело обязанность. Это НЕ штраф
 * @param risk      ответственность за нарушение (статья КоАП + сумма). Может быть null, если ещё не сверено
 * @param sourceUrl ссылка на текст основания (ППРФ). Не путать с risk.sourceUrl — там ссылка на КоАП
 */
public record Obligation(String what, String since, String source, Risk risk, String sourceUrl) {

}
