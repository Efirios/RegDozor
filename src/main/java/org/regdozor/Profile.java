package org.regdozor;

import java.util.List;

/**
 * Профиль пользователя (данные о НЁМ, в отличие от каталога товаров).
 * Пока одно поле — термины его товарной группы (по ним узнаём релевантные релизы ЦРПТ,
 * которые описывают изменения НАЗВАНИЯМИ групп, а не кодами). Позже дорастёт: роль, правовая форма (ИП/ООО).
 *
 * @param groupTerms термины группы (напр. «лёгкая промышленность», «одежда и бельё») — матчинг нормализует их (регистр, ё/е)
 */
public record Profile(List<String> groupTerms) {
}
